-- Ensure truecolor for proper theme rendering
vim.opt.termguicolors = true

-- Catppuccin with transparent_background handles the clean look
-- No custom background overrides needed - let the terminal blur show through
